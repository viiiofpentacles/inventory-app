extends layout

block content
  h1= title

  ul
    each origin in origin_list
        li
            a(href=origin.url) #{origin.country}, #{origin.plantation} plantation
    else
        li There are currently no origins to display.